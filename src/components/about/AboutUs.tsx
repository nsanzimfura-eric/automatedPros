import styles from "./AboutUs.module.scss";

const AboutUs = () => {
    return (
        <div className={`${styles.aboutUs} container d-flex flex-column`}>AboutUs</div>
    )
}

export default AboutUs;